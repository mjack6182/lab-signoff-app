# ============================
# Application Configuration
# ============================
SPRING_APPLICATION_NAME=lab-signoff-backend
SERVER_PORT=8080

# ============================
# MongoDB Configuration
# ============================
# Replace with your actual MongoDB Atlas or local instance values
MONGO_USERNAME=your-username
MONGO_PASSWORD=your-password
MONGO_CLUSTER=cluster0.uiqzzm6.mongodb.net
MONGO_DATABASE=lab_signoff_dev

# Full connection string (recommended way for Spring Boot)
# You can either build this from the variables above
# or paste your MongoDB Atlas URI directly here.
SPRING_DATA_MONGODB_URI=mongodb+srv://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_CLUSTER}/${MONGO_DATABASE}?retryWrites=true&w=majority&appName=Cluster0